package com.unapuno.proyectoprediccion.util

object Constants {
    // =====================================================
    // CONFIGURACIÓN DE SERVIDOR
    // =====================================================
    // Emulador Android → PC host: usar 10.0.2.2
    // Dispositivo real en la misma WiFi: usar IP local del PC (ej: 192.168.1.100)
    // Servidor de producción: usar dominio o IP pública
    // =====================================================
    const val BASE_URL = "http://10.0.2.2/"
}
